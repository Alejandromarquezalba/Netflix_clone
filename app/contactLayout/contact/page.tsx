import React from 'react';
import { ButtonContact, TittleConctact } from '../components';

export default function PageContact() {
  return (
    <div>
      <TittleConctact />
      Aquí va la página de contacto con sus componentes a continuación:
      <ButtonContact />
    </div>
  );
}
